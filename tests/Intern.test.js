const Employee = require("./Employee");

class Intern extends Employee {
  constructor(manager, intern) {
    const manager = manager;
    const intern = intern;

    super(department, jobRole);
    this.department = development;
    this.jobRole = intern;
  }
}

module.exports = {
  Intern,
  intern,
};
