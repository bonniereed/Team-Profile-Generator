const Employee = require("./Employee");

class Manager extends Employee {
  constructor(reportingManager, manager) {
    const reportingManager = reportingManager;
    const manager = manager;

    super(Department, jobRole);
    this.Department = management;
    this.jobRole = manager;
  }
}
module.exports = {
  Manager,
  manager,
};
